export default {
  SET_CAR_DETAIL (state, data) {
    state.carDetail = data
  },

  SET_ERROR (state, data) {
    state.error = data
  },

  SET_LOADING (state, data) {
    state.loading = data
  }
}
